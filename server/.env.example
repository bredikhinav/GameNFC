# Copy to ../.env next to docker-compose.yml
POSTGRES_PASSWORD=CHANGE_ME
FP_ENV=prod
# openssl rand -hex 32
FP_JWT_SECRET=CHANGE_ME
# Domain the API is served on (HTTPS via Caddy), e.g. fantikpay.ru or 158-160-1-2.sslip.io
FP_DOMAIN=fantikpay.ru
# Base of links written to NFC cards; keep the brand domain even before it is bought
FP_PUBLIC_BASE_URL=https://fantikpay.ru
